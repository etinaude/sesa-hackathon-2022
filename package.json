{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "prepare": "husky install",
    "postinstall": "lerna bootstrap",
    "start": "lerna run --parallel start"
  },
  "workspaces": [
    "backend",
    "bus-thoughts"
  ],
  "devDependencies": {
    "lerna": "^5.2.0"
  }
  
}
